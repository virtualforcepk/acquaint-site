# Google Analytics 4 Measurement ID.
# Find it in GA Admin -> Data Streams -> Web -> "Measurement ID" (format: G-XXXXXXXXXX).
# Copy this file to .env, set the value, then rebuild. Empty = analytics stays off (e.g. local dev).
VITE_GA_ID=G-XXXXXXXXXX
